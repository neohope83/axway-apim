pipeline {
   agent any



   stages {
      stage('Build') {
         steps {
            // Get the repository from GitHub
            git 'https://github.com/cwiechmann/sample-api-project.git'

            // Run Maven 
            sh "apim.sh api import -c ./config.json"
         }
      }
   }
}
